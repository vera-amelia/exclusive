'use client';import {useEffect,useState} from 'react';import {dateId} from '@/lib/format';export default function Users(){const [users,setUsers]=useState<any[]>([]);useEffect(()=>{fetch('/api/admin/users').then(r=>r.json()).then(setUsers)},[]);return <div><h1 className="serif" style={{fontSize:38}}>Member</h1><div style={{display:'grid',gap:10}}>{users.map(u=><div key={u.id} className="glass" style={{padding:16,borderRadius:16}}><div style={{display:'flex',justifyContent:'space-between',gap:10}}><div><b>{u.name}</b><div style={{fontSize:13,color:'#8c7c85'}}>{u.email}</div></div><span className="badge">{u.role}</span></div>{u.subscriptions?.[0]&&<div style={{marginTop:10,fontSize:13,color:'#8c7c85'}}>Akses {u.subscriptions[0].tier.name} sampai {dateId(u.subscriptions[0].expiresAt)}</div>}</div>)}</div></div>}
