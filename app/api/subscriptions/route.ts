import {NextResponse} from 'next/server';import {requireUser} from '@/lib/auth';import {prisma} from '@/lib/prisma';export async function GET(){try{const user=await requireUser();const subs=await prisma.subscription.findMany({where:{userId:user.id},include:{tier:true},orderBy:{createdAt:'desc'}});return NextResponse.json(subs)}catch(e:any){return NextResponse.json({error:e.message},{status:401})}}
