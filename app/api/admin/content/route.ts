import {NextResponse} from 'next/server';import {requireAdmin} from '@/lib/auth';import {prisma} from '@/lib/prisma';import {z} from 'zod';
const schema=z.object({title:z.string().min(1),description:z.string().optional(),type:z.enum(['IMAGE','VIDEO']),url:z.url(),thumbnail:z.url(),tierId:z.string(),published:z.boolean().optional()});
export async function GET(){try{await requireAdmin();return NextResponse.json(await prisma.content.findMany({include:{tier:true},orderBy:{createdAt:'desc'}}))}catch(e:any){return NextResponse.json({error:e.message},{status:e.message==='UNAUTHORIZED'?401:403})}}
export async function POST(req:Request){try{await requireAdmin();const body=schema.parse(await req.json());const c=await prisma.content.create({data:{...body,thumbnail:body.thumbnail,published:body.published??true}});return NextResponse.json(c)}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
