import Link from 'next/link';
import { getSessionUser } from '@/lib/auth';
export default async function Header(){const user=await getSessionUser();return <header style={{position:'sticky',top:0,zIndex:30,background:'rgba(255,247,250,.86)',backdropFilter:'blur(14px)',borderBottom:'1px solid #f1e2e8'}}><div className="container" style={{height:72,display:'flex',alignItems:'center',justifyContent:'space-between'}}><Link href="/" className="serif" style={{fontSize:24,fontStyle:'italic',fontWeight:600}}>Vera Amelia</Link><nav style={{display:'flex',gap:10,alignItems:'center'}}>{user?<><Link className="btn btn-ghost" href="/dashboard">Dashboard</Link>{user.role==='ADMIN'&&<Link className="btn btn-primary" href="/admin">Admin</Link>}</>:<><Link className="btn btn-ghost" href="/login">Masuk</Link><Link className="btn btn-primary" href="/register">Daftar</Link></>}</nav></div></header>}
